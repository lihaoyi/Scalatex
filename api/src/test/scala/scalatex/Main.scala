package scalatex
import scalatags.Text.all._
import scalatex.Internals._

object Main {
  def main(args: Array[String]): Unit = {
//    scalatex.stages.Parser.apply("omg @notInScope lol"))
//    twRuntimeErrors("@p{@Seq(1, 2, 3).foldLeft(0)}")
  }
}

